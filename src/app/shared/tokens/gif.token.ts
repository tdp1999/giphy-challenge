import { InjectionToken } from '@angular/core';

export const GIF_LIMIT = new InjectionToken<number>('GIF_LIMIT');
export const RELATED_GIF_LIMIT = new InjectionToken<number>(
  'RELATED_GIF_LIMIT'
);
